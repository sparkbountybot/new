name: Send Bounty Proposals

on:
  push:
    branches:
      - main
    paths:
      - 'data/bounty_manifest.json'
      - 'proposals/*.md'
    # Only trigger on non-ACTION pushes (skip auto-pushes from this action)
    # This prevents infinite loops when the action commits results back
  workflow_dispatch:

# Skip this workflow when commits come from sparkbountybot[bot] (GitHub's automated bot)
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  issues: read

jobs:
  send-proposals:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Skip runs that are automatically triggered by the bot itself (avoid loops)
    if: "!contains(github.event.head_commit.author.username, 'sparkbountybot')"
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib

      - name: Send bounty proposals via Gmail
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          GMAIL_EMAIL: sparkbountybot@gmail.com
        run: python scripts/send_proposals.py

      - name: Commit results
        if: always()
        run: |
          git config user.name "sparkbountybot"
          git config user.email "sparkbountybot@gmail.com"
          git add data/sent_log.json data/bounty_manifest.json
          git commit -m "📧 Email delivery results [run #${{ github.run_number }}]" --no-verify || echo "No changes to commit"
          git push --quiet || echo "Push failed (may be empty commit)"
